----------------------------------Windos-----------------------------------------------------------
#CPU
Get-CimInstance Win32_Processor | Select Name,NumbersOfCores,NumberOfLogicalProcessor,MaxClockSpeed

#Dischi
Get-PhysicalDisk | Select FriendlyName,Mediatype,BusType,Size

---------------------------------Linux-------------------------------------------------------------

#CPU
lscpu

#RAM
free -h

#Dischi
lsblk -o NAME,SIZE,TYPE,MODEL,TRAN

#Scheda madre, RAM e firmware
sudo dmidecode -t baseboard -t memory -t bios

#Schede PCI/PICIe
lspci | grep -Ei 'vga|3d|network|audio'
